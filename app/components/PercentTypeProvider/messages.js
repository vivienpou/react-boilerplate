/*
 * PercentTypeProvider Messages
 *
 * This contains all the text for the PercentTypeProvider component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.PercentTypeProvider';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PercentTypeProvider component!',
  },
});

/*
 * ProgressBarCell Messages
 *
 * This contains all the text for the ProgressBarCell component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ProgressBarCell';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProgressBarCell component!',
  },
});

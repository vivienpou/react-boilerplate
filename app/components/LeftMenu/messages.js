/*
 * LeftMenu Messages
 *
 * This contains all the text for the LeftMenu component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.LeftMenu';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LeftMenu component!',
  },
});

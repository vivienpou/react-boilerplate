/*
 * HighlightedCell Messages
 *
 * This contains all the text for the HighlightedCell component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HighlightedCell';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HighlightedCell component!',
  },
});

/*
 * AdminProducts Messages
 *
 * This contains all the text for the AdminProducts container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AdminProducts';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AdminProducts container!',
  },
});
